#include<stdio.h>
int main()
{    int a;
     printf("enter the value of a ");
     scanf("%d",&a);
     if(a%2==0)
     {
     	printf("the no. is even",a);
	 }
	 else
     printf("the no. is odd",a);
     
    
	return 0;
}
