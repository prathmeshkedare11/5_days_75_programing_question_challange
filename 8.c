#include<stdio.h>
int main()
{
   	int integer;
	long   F;
	long long D;
	 long double ch;
	 double a;  
	printf(" the size is %zu bytes \n",sizeof(integer));
		printf(" the size is %zu bytes\n ",sizeof(F));
			printf(" the size is %zu bytes \n",sizeof(D));
				printf(" the size is %zu bytes\n ",sizeof(ch));
					printf(" the size is %zu bytes ",sizeof(a));
	
	return 0;
}
